# --- Bí mật: KHÔNG bao giờ đẩy khóa API lên GitHub ---
.env

# Công cụ cấp key + khoá bí mật — TUYỆT ĐỐI không commit
HD CAP KEYGEN/
hungarch_private.pem
hungarch_keygen.py

# Môi trường ảo Python
.venv/
venv/
__pycache__/
*.py[cod]

# Ảnh đã render & metadata (sản phẩm chạy ra, không cần version)
outputs/*
!outputs/.gitkeep

# Hệ điều hành / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Preview / launch config cục bộ
.claude/
